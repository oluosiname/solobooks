<%# TODO Decide if we're replacing or just prepending the new income %>
<%= turbo_stream.prepend "flash", partial: "partials/flash_messages" %>
<turbo-stream action="prepend" target="transactions-list">
  <template>
    <%= render partial: "transactions/transaction", locals: { transaction: @transaction}%>
  </template>
</turbo-stream>